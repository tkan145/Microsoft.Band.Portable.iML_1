<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
	x:Class="Microsoft.Band.Portable.iML.Views.LogCellView">
	<ContentView.Content>
		<Grid Padding="16,5" ColumnSpacing="{StaticResource StandardSpacing}">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
              </Grid.ColumnDefinitions>

              <StackLayout Grid.Column="1" 
                           VerticalOptions="Center"
                           Spacing="{StaticResource SmallSpacing}">
               <Label Style="{DynamicResource iMLListItemTextStyle}" 
                      Text="{Binding State}"/>
                <Label Style="{DynamicResource iMLListItemDetailTextStyle}"
                       Text="{Binding Action}"/>
				<Label Style="{DynamicResource iMLListItemDetailTextStyle}"
                       Text="{Binding Reward}"/>
              </StackLayout>
        
            </Grid>
	</ContentView.Content>
</ContentView>
